@model List<Module.DB.tblCampus>

@{
    ViewBag.Title = "Campus";
    ViewBag.Subtitle = "List";

}

<script>

    function show() {
        window.open()
    }

</script>

@section Breadcrumbs {
    @Html.Partial("_Breadcrumbs", new List<Breadcrumb>() { new Breadcrumb { Description = ViewBag.Title } })
}

<div class="block">
    <div class="head bg-default bg-light-ltr noradius">
        <h2>@ViewBag.Title</h2>
        <div class="side pull-right">
            <ul class="buttons">
                <li>
                    <button class="btn btn-success btn-create" onclick="$.fn.list('modalCreate', this)">Create</button>
                </li>
            </ul>
        </div>
    </div>
    <div class="content">

        <table class="display table table-bordered table-striped table-list" cellspacing="0" width="100%"
               modal-target="#modalView" modal-start="view"
               modal-view-url="/HRModule/Campus/Details/{0}" modal-edit-url="/HRModule/Campus/Edit/{0}" modal-delete-url="/HRModule/Campus/Delete" modal-create-url="/HRModule/Campus/Create">
            <thead>
                <tr>
                    <th class="idcol">
                        Id
                    </th>
                    <th>
                        Campus
                    </th>
                    <th>
                        Address
                    </th>
                    <th>
                        Contact Number
                    </th>
                    <th>
                        Payroll Summary Signatory
                    </th>
                    <th>
                        Position
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Module.DB.tblCampus item in Model)
                {
                    <tr record-id="@item.CampusID">
                        <td>
                            @Html.DisplayFor(modelItem => item.CampusID)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Campus)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CampusAddress)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ContactNumber)
                        </td>
                       <td>
                            @Html.DisplayFor(modelItem => item.PayrollSummarySignatory)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SignatoryPosition)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@Html.Partial("~/Views/Shared/modals/_ModalView.cshtml", "modalView")
